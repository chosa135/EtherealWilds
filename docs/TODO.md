# TODO

Codexへ依頼しやすいよう、小さめの作業単位で並べています。

## 優先度 高

### 1. 所持品満杯時の入れ替えUI

現状、報酬を受け取るユニットの所持品が満杯だと、そのユニットには渡せません。

実装案:

- 報酬選択後、満杯のユニットも選べるようにする。
- 満杯のユニットを選んだ場合、捨てる所持品を選ぶ画面を出す。
- 選んだ所持品を破棄し、報酬をその枠に入れる。
- 破棄確認は最初は不要でもよい。

主な編集先:

- `src/main.ts`
- `src/logic/inventory.ts`
- `docs/DESIGN.md`

### 2. 報酬候補の重複抑制

現状、報酬候補3つに同じ `itemMasterId` が重複する可能性があります。

実装案:

- 3候補の中では同じ `itemMasterId` を避ける。
- テーブルが小さく、どうしても候補が足りない場合だけ重複を許す。
- レアリティ率、カテゴリ率は変えない。

主な編集先:

- `src/logic/rewards.ts`

### 3. 戦闘中ではなくワールドマップで能力強化アイテムを使う

現状、能力強化アイテムは戦闘中の「道具」から使用できます。

実装案:

- ワールドマップで味方一覧を開けるようにする。
- 味方を選んで所持品を表示する。
- 能力強化アイテムはワールドマップ上で使用可能にする。
- 戦闘中は回復系のみ使用可能にするか、能力強化アイテムを非表示にする。

主な編集先:

- `src/main.ts`
- `src/logic/inventory.ts`
- `docs/DESIGN.md`

## 優先度 中

### 4. 地形の回避補正

森の地形に戦術的意味を足します。

実装案:

- 森にいる防御側は回避+10。
- 表示命中に反映する。
- 戦闘予測にも反映する。

主な編集先:

- `src/logic/combat.ts`
- `src/main.ts` から地形情報を渡す設計が必要
- `docs/DESIGN.md`

### 5. 報酬テーブルの深度補正

序盤・中盤・終盤で報酬の質を変えます。

実装案:

- 現在のバトルインデックスを報酬生成に渡す。
- 序盤はコモン多め、終盤はアンコモン・レアを少し増やす。
- まずは固定率のままでもよい。実装する場合はdocsに明記する。

主な編集先:

- `src/logic/rewards.ts`
- `src/data/rewards.ts`
- `src/main.ts`

### 6. 敵AIの改善

現在の敵AIは、攻撃可能ならHPの低い相手を狙い、無理なら近づくだけです。

実装案:

- 撃破可能な相手を優先する。
- 被反撃リスクが低い相手を優先する。
- 弓・魔法は射程を活かして近づきすぎない。

主な編集先:

- `src/logic/enemyAI.ts`
- `src/logic/combat.ts`

### 7. 成長処理の分離

成長処理は現在 `main.ts` にあります。

実装案:

- `src/logic/growth.ts` を作る。
- `weightedStat`, `addExp`, `levelUp` 周辺を移す。
- ポップアップ生成との接続を整理する。

主な編集先:

- `src/logic/growth.ts`
- `src/main.ts`

## 優先度 低

### 8. クラスチェンジ候補判定

ラン中の成長偏りに応じて、上級職候補を出します。

未決定事項:

- いつクラスチェンジするか。
- 条件をどうするか。
- 武器種・能力補正をどう扱うか。

### 9. 分岐ワールドマップ

一本道から、2択または3択のノード選択へ拡張します。

候補ノード:

- 通常戦闘
- 強敵
- 宝
- 休憩所
- 商人
- イベント

### 10. 受け渡し・輸送隊・店

所持品4枠の制約をどこまで緩めるかは慎重に決める必要があります。

- 受け渡しだけ実装する。
- 輸送隊を実装する。
- 店や売却を実装する。

いずれもUIが大きくなるため、先に報酬の入れ替えUIを実装する方が安全です。
